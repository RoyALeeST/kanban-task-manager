import { LineTroughTextDirective } from './line-trough-text.directive';

describe('LineTroughTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LineTroughTextDirective();
    expect(directive).toBeTruthy();
  });
});
